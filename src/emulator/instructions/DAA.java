package emulator.instructions;

import emulator.Emulator;

public class DAA implements Instruction {
    @Override
    public void operate(Emulator state) {
//        throw new UnimplementedInstructionError();
    }
}
